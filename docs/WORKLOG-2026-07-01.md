# WORKLOG 2026-07-01 — 포스터 4쪽 → 3쪽 개편

## 요청
`LG_부트캠프_13기_C반_포스터(4팀).pptx`를 3쪽으로 압축하고 내용을 보강.

## 변경 요약
- **4쪽 → 3쪽 압축.** 원본은 `LG_부트캠프_13기_C반_포스터(4팀)_backup_4면.pptx`로 백업.
- 생성 스크립트: `scratchpad/build_poster.py`(기존 4쪽 생성 스크립트를 3쪽으로 개편). python-pptx 사용, 디자인 토큰(다크네이비 `0F1626`·액센트 레드 `E4002B`·아이스블루 `CADCFC`·라이트카드 `F3F5F8`, Cambria+Malgun Gothic) 그대로 계승.
- 렌더/QA는 PowerPoint COM(PNG export)으로 수행(이 환경 LibreOffice 없음). 3쪽 모두 오버플로·겹침 없음 확인.

### P1 — 배경 & 실제 사용사례 (전면 개편)
- 글 위주였던 문제/핵심통찰/해법 3카드를 축약: 문제 1줄 + 핵심 통찰 다크 하이라이트 1카드.
- **실제 @refine before/after 사용사례 추가**(사용자 캡처 반영): 원본 `"레이트 리미터 만들어줘"` → 정제본(역할·임무·필수 포함 4항목·출력 형식). 의존성 적고 LLM 사고력을 키우는 과제로 선정.

### P2 — 아키텍처 (유지)
- 기존 구조 유지. FE 카드 하단 문구를 내부표기(`R-EX-01~11`)에서 청중용(`순수 JS · 런타임 의존성 0 · 승인 게이트 기반 UX`)으로 순화.

### P3 — 파이프라인 & 성과
- **설계 불변식 6칩 제거**(fail-open·명시 호출만 등 — 내부 설계라 청중용 아님).
- 자리 대체 = **결과 성과 지표**(문서 기준 정정):
  - `24` 요구사항 전건 구현 (명세서 R-01~R-24 = 24개; 기존 오기 "7/30" 정정)
  - `46 / 46` 자동 테스트 통과 (junit 실측: AI 3 + BE 18 + EXT 25; 기존 "25/25"는 확장만)
  - `651 · 4096` 템플릿·임베딩 차원 (manifest)
  - `0` FE 런타임 의존성
  - 부가: 확장 라인 커버리지 92%(coverage.lcov)

## 근거 문서
- `SW_Bootcamp_13기_C반_4팀_요구사항_명세서.md` (R-01~R-24)
- `result_file/test-results/{program_AI,program_backend,program_extension}/junit.xml`, `.../program_extension/summary.txt`
- `result_file/README.md` (manifest 651 / embedding_dim 4096)

## 산출물
- `LG_부트캠프_13기_C반_포스터(4팀).pptx` (3쪽, 신규)
- `LG_부트캠프_13기_C반_포스터(4팀)_backup_4면.pptx` (기존 4쪽 백업)

## 후속(선택)
- PDF 재변환 제출 필요 시 PowerPoint COM으로 재생성.

---

# 추가 개편 — 3쪽 → 2쪽 (폰트 포함 저장)

## 요청
사용자가 직접 pptx 문구 일부를 수정한 뒤, 3가지 수정 + 2쪽 압축 + 글꼴 포함 저장 요청.

## 사용자 수정 문구 반영 (현재 pptx에서 추출해 사용)
- 부제: "VSCode Copilot 확장에서 간편하게 사용하는 프롬프트 개선 서비스"
- 태그라인: "@refine — 간단한 프롬프트를 구조화된 프롬프트로"
- "핵심 통찰" → **"핵심 인사이트"**
- 필수 포함 ②: "…단일 인스턴스 한계 극복. 한국어 주석 첨부"
- **팀원 명단(우상단, 사용자 추가): 김태용/김민정/박혜림/홍창표 연구원** — 보존
- P2 "@refine 참가자": "정제된 프롬프트 체크 + 5선택지 제시" / 사이드바: "· diff" 제거
- P3 제목: "프롬프트 향상 매커니즘 — 4단계"

## 3가지 수정
1. **P1 상단 구조도 교체**: 정제만 빨간색이던 4칩(정제→검색→리랭킹→생성)을 **입력→과정→향상 구조도**로 대체 — `[사용자 입력] → [정제 › 검색 › 리랭킹 › 생성] → [향상된 프롬프트]`.
2. **P2 하단 AI Infra / Vector DB 사실 나열 삭제**.
3. **P3 하단 결과 성과 파트 통째 삭제**.

## 2쪽 구성
- **P1**: 배경 + 구조도 + 실제 @refine before/after 사용사례
- **P2**: 아키텍처(FE 확장) + POST /refine + BACKEND 파이프라인 4단계 + @refine UX 5선택지 (AI/VDB·성과 없음)

## 빌드/저장
- 스크립트: `scratchpad/build_poster2.py` (python-pptx). QA는 PowerPoint COM PNG export.
- **글꼴 포함 저장**: PowerPoint COM `SaveAs(path, 24, msoTrue)` — `ppt/fonts/*.fntdata` 10개 파트 임베딩 확인(Cambria/Calibri/Malgun Gothic 정·볼드), `embeddedFontLst` 존재. 파일 1.43MB.
- QA: 2쪽 모두 오버플로·겹침 없음(초판에서 P1 제목 줄바꿈·P2 취소행/푸터 겹침 발견 → 제목 36pt 단일행·세로간격 압축으로 수정).

## 산출물(갱신)
- `LG_부트캠프_13기_C반_포스터(4팀).pptx` (2쪽, 폰트 포함)
- 백업: `..._backup_4면.pptx`(원 4쪽), `..._backup_3면.pptx`(직전 3쪽)
