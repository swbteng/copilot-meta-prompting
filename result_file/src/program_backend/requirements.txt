# program_backend dependencies (API + pipeline + retrieval + offline indexing).
# Single deployment requirements file; versions are pinned from the backend .venv.
fastapi==0.138.1
uvicorn[standard]==0.49.0
requests==2.34.2
chromadb==1.5.9
openai==2.44.0
tqdm==4.68.3
