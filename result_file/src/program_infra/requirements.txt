# program_AI 의존성 (GPU 서버에서 vLLM 모델 서빙).
# serve_*.sh 가 vllm 으로 모델을 서빙하고, scripts/test_apis.py 가 requests 로 세 엔드포인트를 점검한다.
# 기존 /workspace/vllm/.venv 와 "같은 버전"으로 핀했다(직접 새 venv 에 설치). vllm 설치 시
# 호환되는 torch/transformers 등은 아래 참고 버전으로 함께 끌려온다.
vllm==0.19.1
requests==2.34.2

# 참고: 위 vllm 설치 시 함께 들어오는 핵심 버전 (직접 핀할 필요는 없음)
#   torch==2.10.0  transformers==5.12.1  fastapi==0.138.0  uvicorn==0.49.0
#   numpy==2.2.6   huggingface-hub==1.20.1  tokenizers==0.22.2
