# requirements.txt — swbc-prompt-refiner
#
# 이 프로젝트는 **순수 JavaScript VS Code 확장**이며, 파이썬/런타임 의존성이 없습니다.
#   - 런타임: Node.js 내장 모듈(http / https / url)만 사용 — 설치할 패키지 없음(의존성 0).
#   - 패키징(build.sh): **하이브리드**. Tier 1(선호) Node/npm 있으면 @vscode/vsce(package.json
#                       devDependencies, 핀 3.9.2)로 표준 빌드. Tier 2(폴백) Node 없거나 vsce 실패면
#                       Windows 기본 내장 PowerShell만으로 .vsix(=OPC ZIP) 직접 생성(설치·네트워크 0).
#                       => '아무것도 없는 머신에서도 빌드 가능'. (빌드 의존성은 pip가 아니라 package.json에 선언.)
#   - (참고) 테스트(node --test)에도 Node.js ≥ 20.13 필요.
#
# 따라서 pip로 설치할 파이썬 모듈이 없습니다(의도적으로 비어 있음).
# (과거 mitmproxy 기반 프록시 구조에서 쓰던 requirements는 폐기·삭제되었습니다.)
#
# 실행/패키징 방법은 RUN.md 참고.
