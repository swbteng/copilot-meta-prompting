# .env.example — .env 템플릿(이 파일만 커밋, 실제 .env는 gitignore).
#   복사해서 쓰기:  cp .env.example .env
#
# 이 확장이 읽는 환경변수는 REFINE_API_URL 하나뿐(src/refiner.js).
# 비우거나 .env가 없으면 코드에 내장된 기본 서버 URL을 쓴다.

REFINE_API_URL=https://<정제-서버-호스트>/refine
