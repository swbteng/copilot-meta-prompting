# 테스트 실행용 의존성 (버전 고정). 런타임 requirements 와 분리한다.
#
# 적용 대상: program_backend / program_AI 의 pytest 테스트.
#   - program_backend 테스트는 백엔드 런타임 모듈을 import 하므로 그 런타임 패키지가 필요하다.
#   - program_AI 테스트는 HTTP 호출만 하므로 requests 만 있으면 된다.
#
# 주의(의도적 제외):
#   - vllm        : 모델 '서빙' 전용(GPU). 테스트 클라이언트엔 불필요.
#   - scikit-learn: 오프라인 벡터 시각화 인덱싱 전용. 테스트엔 불필요(+Python 3.11 이상 요구).
#
# 설치:  python -m pip install -r tests/requirements-test.txt

# --- 백엔드 런타임(테스트가 import) — src/program_backend/requirements.txt 와 동일 핀 ---
fastapi==0.138.1
requests==2.34.2
chromadb==1.5.9
openai==2.44.0

# --- 테스트 프레임워크/클라이언트 ---
pytest==9.1.1
httpx==0.28.1
